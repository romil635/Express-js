<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f4f4f4, #e0e0e0);
            margin: 0;
            padding: 20px;
            height: 100vh; /* Full viewport height */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center; /* Center content vertically */
            overflow: hidden;
            animation: fadeIn 2s ease-out;
        }

        h1 {
            font-size: 2rem;
            color: #333;
            margin-top: 0;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
            opacity: 0;
            font-size: 80px;
            transform: translateY(-20px);
            animation: slideIn 1.5s ease-out 0.5s forwards;
        }

        p {
            font-size: 30px;
            padding-top: 20px;
        }

        .table-container {
            width: 100%;
            max-width: 1000px;
            height: 70vh; /* Set the height relative to the viewport */
            overflow-y: auto; /* Add vertical scrollbar */
            background-color: #fcfbfb;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            transform: scale(0.95);
            opacity: 0;
            animation: tableFadeIn 2s ease-out 1s forwards;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes tableFadeIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        th, td {
            padding: 16px;
            text-align: left;
            border-bottom: 1px solid #ddd;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        th {
            background: linear-gradient(135deg, #f3d5fe, #dc9cfc);
            color: #fff;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        td {
            background-color: #f9f9f9;
        }

        tr:nth-child(odd) td {
            background-color: #fafafa;
        }

        tr:hover td {
            background-color: #f1f1f1;
            color: #333;
        }

        tbody tr:last-child td {
            border-bottom: none;
        }

        tr td button {
            width: 100px;
            background-color: antiquewhite;
            border-radius: 20px;
        }

        tr td button:hover {
            background-color: #c1ecef;
        }

        tr td input {
            width: 100px;
            height: 20px;
            padding: 10px;
            border-radius: 20px;
            border: none;
            background-color: red;
        }

    </style>
</head>
<body>
    <p>..🙏..WelCome To Todo List Pages..🙏..</p>
    <h1>-:Todo List Pages:-</h1>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>Profile</th>
                    <th>User Name</th>
                    <th>Email</th>
                    <th>Mobile No</th>
                    <th>Edit</th>
                    <th>Delete</th>
                    <th>Pending</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(function(user) { %>
                    <tr>
                        <td><%= user.profile %></td>
                        <td><%= user.username %></td>
                        <td><%= user.email %></td>
                        <td><%= user.mobileNo %></td>
                        <td><%= user.Edit %><button>Edit</button></td>
                        <td><%= user.Delete %><button>Delete</button></td>
                        <td><input type="checkbox"></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
